var court = document.getElementById('game');
var context = court.getContext('2d');
// return setInterval(draw, 10);
// need to write draw function





// context.fillStyle = white;
// context.beginPath();
// // arc(x, y, radius, 0, Pi* 2, true)
// context.arc(150, 75, 7, 0, Math.PI*2, true);
// context.fill();

// var gradient = context.createRadialGradient(85, 88, 5, 88, 90, 69);
// gradient.addColorStop(0, 'green');           
// gradient.addColorStop(1, 'white');
// context.fillStyle = gradient;
// context.arc(150, 75, 7, 0, Math.PI*2, true);
// context.fill();
// var paddle = {
// 	speed: 256, // movement in pixels per second
// };

// var points = 0;

